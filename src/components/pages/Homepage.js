import React from 'react';
import Button from '../atoms/Button';
import Input from "../atoms/Input";

const Homepage = () => {
  return (
    <div>
      Homepage
      <Button>Add smt</Button>
      <Input/>
    </div>
  );
};

export default Homepage;
